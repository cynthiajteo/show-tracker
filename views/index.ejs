<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <%- include('./partials/head.ejs')%>
    </head>
    <body>
        <div class="container">
            <h1>Show Tracker</h1>
            <h2>
                <% if(currentUser) { %> Welcome <%=currentUser.username%>!
                <br />
                <h2>
                    <a class="waves-effect waves-light btn" href="/app"
                        >Show me my shows!</a
                    >
                </h2>
                <form action="/sessions?_method=DELETE" method="POST">
                    <input
                        type="submit"
                        value="Logout"
                        class="waves-effect waves-light btn"
                    />
                </form>
            </h2>
            <% } else { %>
            <h2>Please</h2>
            <a class="waves-effect waves-light btn" href="/users/new">
                <button>Sign Up</button>
            </a>
            <h3>or</h3>
            <a class="waves-effect waves-light btn" href="/sessions/new">
                <button>Log In</button>
            </a>
            <% } %>
        </div>
    </body>
</html>
