<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <%- include('./partials/head.ejs')%>
    </head>
    <body>
        <div class="header">
            <div class="header-title">
                <img
                    src="https://img.icons8.com/ios/452/retro-tv.png"
                    alt="tv icon"
                />
                <a href="/">Show Stopper</a>
            </div>
        </div>
        <div class="container">
            <div class="welcome"></div>
            <h2>
                <% if(currentUser) { %> Welcome, <%=currentUser.username%>!
                <br />
                <h2>
                    <a class="dashboard" href="/app">
                        <button>Shows Dashboard</button>
                    </a>
                </h2>
                <form action="/sessions?_method=DELETE" method="POST">
                    <input type="submit" value="Logout" id="logout-btn" />
                </form>
            </h2>
            <% } else { %>
            <h2>Are you new here?</h2>
            <a class="waves-effect waves-light btn" href="/users/new">
                <button>Sign Up</button>
            </a>
            <h3>Welcome Back!</h3>
            <a class="waves-effect waves-light btn" href="/sessions/new">
                <button>Log In</button>
            </a>
            <% } %>
        </div>
    </body>
</html>
